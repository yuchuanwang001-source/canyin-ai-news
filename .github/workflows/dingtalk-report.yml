name: 钉钉日报
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "已迁移到 daily-report.yml，此工作流不再发送钉钉消息。"
